<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.H5AppTemplate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="Theme.H5AppTemplate" parent="Base.Theme.H5AppTemplate" />
</resources>